using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapVFXOptimizer : MonoBehaviour
{
    /*Transform player;
    public GameObject[] VFXs;
    public float VFXSize = 20f;
    private Vector3[] vfxsPositions;
    private int vfxCount = 0;
    void Start()
    {
        player = PlayerController.Instance.transform;
        vfxCount = VFXs.Length;
        vfxsPositions = new Vector3[vfxCount];
        for (int i = 0; i < vfxCount; i++)
        {
            vfxsPositions[i] = VFXs[i].transform.position;
        }
    }

    void Update()
    {
        GameObject closestVFX = null;
        for (int i = 0; i < vfxCount; i++)
        {
            if (Vector3.Distance(player.position, vfxsPositions[i]) < VFXSize)
            {
                closestVFX = VFXs[i];
                break;
            }
        }
    }*/
}
